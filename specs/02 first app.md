读取必要文档，添加第一个支持安装的软件 wireguard。

1. 由于是第一个，所以要想好代码组织方式，遵循好的软件开发原则，以提高代码复用率和鲁棒性
2. 安装应以判断不同的系统，来确定最佳的安装方式，安装方式可能有：系统包管理器、brew、mise、asdf 等
   对于非系统包管理器，要判断是否存在，不存在的话则跳过该方式，可行的安装方式让用户确认（同时展示必要信息，比如什么工具安装、安装在哪里等）
